# Codex Prompt Example

Apply Command Budget and Source Reading Economy.

- Use `investigate.ps1` instead of more than 3 related search commands.
- If the target file is already known, use Known File Reading Protocol.
- Do not inspect the same file with repeated `read-window.ps1` calls.
- Use at most 8 shell/helper commands unless you stop and summarize why more are needed.
- For noisy commands, use `run-compact.ps1` or `test.ps1`.
- For UTF-8 pasted text, use `read-text.ps1` or `Get-Content -Raw -Encoding UTF8`.

Task:
<describe task here>
